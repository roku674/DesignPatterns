const Client-SideDiscovery = require('./Client-SideDiscovery');

const pattern = new Client-SideDiscovery({ example: 'configuration' });
const result = pattern.execute();
console.log('Result:', result);
